import { RatingpipePipe } from './ratingpipe.pipe';

describe('RatingpipePipe', () => {
  it('create an instance', () => {
    const pipe = new RatingpipePipe();
    expect(pipe).toBeTruthy();
  });
});
